export
fn mymax(reg u64 array, reg u64 size) -> reg u64{

  reg u64 max,i;

  i = 0;
  max = -1;

  while(i < size){

  	max = [array + 0] if max < [array + 0];
    array += 64;
    i += 1;
	}

  return max;
	
}